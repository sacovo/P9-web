---
interface Props {
  eyebrow: string;
  title: string;
  lead: string;
}

const { eyebrow, title, lead } = Astro.props;
---

<section class="shell pt-14! pb-2!">
  <p class="eyebrow">{eyebrow}</p>
  <h1>{title}</h1>
  <p class="mt-5 max-w-[68ch] text-[1.05rem] text-grey-4">{lead}</p>
</section>
